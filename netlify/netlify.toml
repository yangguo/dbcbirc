[build]
  publish = ".next"
  command = "npm install && npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "22"
  NPM_VERSION = "10"

# This is a standalone Next.js app with its own API routes
# No external API dependencies needed